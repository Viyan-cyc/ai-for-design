# 页面自适应方案(L1+L2 默认,B 端流式自适应)

> 持久化于 2026-09-15,基线 commit `c8db5de`(pure 分支)。
> 口径:原型受众是设计师/评审,桌面浏览器打开;自适应不改变 px 单位体系。

## 口径分级(B 端语境)

| 级别 | 含义 | 覆盖场景 | 决策 |
|---|---|---|---|
| **L1 宽度自适应** | 内容区 100% 流式,1280-1920 都好看,窄了表格内部出横向滚动条 | 1366/1440/1920 笔记本+显示器 | **默认必做** |
| **L2 栅格断点** | KPI 卡片行、筛选区、卡片网格在窄窗口下自动换行/降列数 | 窗口拖窄、半屏分屏 | **默认必做**(成本极低) |
| **L3 窄屏布局** | <992 时侧导航收起、表单 label 置顶、双栏变单栏 | 平板横屏、小窗口 | 按需,不做默认 |
| 移动端 H5 | 数据密集控制台改移动布局 | 手机 | **明确不承诺** |

## 技术裁决

**Element Plus 不会自动自适应**:组件样式 px 钉死、桌面优先;唯一自带响应式能力是 `el-row/el-col` 栅格 `:xs/:sm/:md/:lg/:xl` 断点属性,需主动使用。表格流式 + 内部滚动是 EP 内建能力。

**px 保留**,流式靠四样东西:
1. 流式容器:`.page-root` 不写死宽度
2. EP 栅格断点:成排卡片/表单项 `el-row :gutter="16"` + `el-col` 断点
3. flex/grid + `min()`/`max-width`:对话框 `width="min(720px, 92%)"` 等
4. 媒体查询(Less 内 `@media`):仅 L3,断点对齐 EP 官方五档(`<768 / ≥768 / ≥992 / ≥1200 / ≥1920`),禁止自造断点

**明确否决的方案**(写进规范防 AI 走偏):
- ❌ `postcss-px-to-viewport` / rem(lib-flexible)——H5/大屏做法,破坏 EP px 组件样式
- ❌ `zoom` / `transform: scale` 整页缩放——大屏方案,文字糊、交互坐标错位
- ❌ 页面级 `min-width: 1440px` + 整页横滚——数据密集页可例外,不做默认

## 规范条款(拟新增「自适应规范」节)

1. **容器**:`.page-root` 只用 padding,不设 width/min-width;B 端壳 `el-aside` 固定 208-220px(可折叠),`el-main` 流式
2. **成排卡片**(KPI 行、统计卡):`el-row :gutter="16"` + `el-col` 断点降列,基准 `:xs="24" :sm="12" :md="8" :lg="6"`
3. **表格**:`el-table` 默认流式,禁止给表格写死 width;列用 `min-width` 不用 `width`,空间不足时表格内部出滚动条
4. **筛选行**:inline form + `flex-wrap: wrap`,控件定宽不写死
5. **对话框**:`width="min(720px, 92%)"` 模式(按内容选 480/720/960 基准)
6. **媒体查询**仅 L3 使用;断点五档对齐 EP,禁止自造断点数值
7. **截图转码例外**:按截图保真还原布局,但仍按上述纪律做流式,不照抄截图固定像素宽度

## 阶段一(文档层,本轮执行)

- [x] code-conventions.md 新增「自适应规范」节(7 条 + KPI 行示范代码块),节次插在「§3 样式规范」之后顺延编号
- [x] SKILL.md HARD RULES 加 1 行:「页面流式自适应(细则见 code-conventions『自适应规范』)」——不展开

## 阶段二(代码层,另行立项)

- [ ] `init.mjs` starter 页:KPI 行改用栅格断点写法(示范代替说教,收益最大)
- [ ] `build.mjs`:WARN 规则——`.page-root`/页面级容器固定像素 `width:` 或页面级 `min-width` 警告
- [ ] `smoke.mjs`(可选):双视口检查(1920 + 900)各渲染一次,断言无页面级横向溢出(表格内部滚动除外)

## 执行顺序

阶段一 → 生成 1-2 个页面实测验证 → 阶段二 starter 示范 → build WARN → smoke 双视口(可选)
